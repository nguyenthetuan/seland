import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  boxSelect: {
    flexDirection: 'row',
    marginTop: 10,
  },
  btnSelect: {
    // borderRadius: 5,
    height: 26,
    marginLeft: 10,
    padding: 0,
    width: 85,
  },
  txtSelect: {
    fontSize: 14,
    lineHeight: 18,
  },
});

export default styles;

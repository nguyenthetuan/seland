import { StyleSheet } from 'react-native';
import COLORS from './colors';

export const appStyles = StyleSheet.create({
  flex: {
    flex: 1,
  },
  background: { backgroundColor: COLORS.WHITE, flex: 1 },
});
